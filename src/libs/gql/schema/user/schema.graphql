type Query {
  user(id: String): User
}

type Mutation {
  createUser(input: CreateUserInput!): CreateUserPayload
}

type User {
  id: ID!
  name: String!
  age: Int!
}

input CreateUserInput {
  name: String!
  age: String!
}

type CreateUserPayload {
  user: User!
}
